# Project Custom Rules

## Logging Requirements
- **Timestamp on First Row**: Whenever a log file (e.g., `log.md`) is written or updated, the very first row/line of the log must contain a TIMESTAMP (e.g., `# Last Updated: YYYY-MM-DD HH:MM:SS`).
